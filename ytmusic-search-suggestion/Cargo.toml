[package]
name = "ytmusic-search-suggestion"
version = "0.1.0"
edition = "2021"
description = "YouTube Music search suggestions"

[dependencies]
bex-core = { path = "bex-core", features = ["suggestion"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
urlencoding = "2.1"

[lib]
crate-type = ["cdylib"]

[package.metadata.component]
package = "component:search-suggestion-provider"

[package.metadata.component.dependencies]

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
panic = "abort"
debug = false